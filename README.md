# MPI4PY Projects Collection

Этот репозиторий содержит **13 практических работ**, выполненных с использованием библиотеки **mpi4py**, которая реализует интерфейс MPI (Message Passing Interface) для параллельных вычислений в Python.

Каждый файл представляет собой отдельную задачу, иллюстрирующую различные механизмы обмена сообщениями, организации вычислений и распределения нагрузки между процессами.

## 🚀 Запуск программ

Все проекты запускаются через `mpiexec` или `mpirun`. Общий шаблон:

```bash
mpiexec -n <количество_процессов> python <имя_файла>.py
```

Например:

```bash
mpiexec -n 5 python mp1.py
```

## 📂 Структура репозитория

В корневой директории расположены следующие файлы и папки:

* `.git/` — служебные файлы Git
* `.idea/` — конфигурации среды разработки
* `.venv/` — виртуальное окружение Python
* `Results.dat` — результаты вычислений
* `mp1/`, `mp2/`, ..., `mp13/` — папки с дополнительными файлами для некоторых задач
* Основные скрипты задач: `mp1.py`, `mp2-1.py`, ..., `mp13-2.py`


> Структура заданий может включать дополнительные папки `mpX/`, содержащие вспомогательные файлы или тестовые данные.

---

## 📌 Требования

* Python 3.10+
* Установленная библиотека **mpi4py**
* Установленный MPI‑дистрибутив (OpenMPI, MPICH и т.п.)

Установка:

```bash
pip install mpi4py
```
